package sample;

import java.util.ArrayList;

public class RungeKutta2p extends CMethod {
    public RungeKutta2p(Point _currentPoint, int _p, ArrayList<Point> _testPoints, IFunction _someFunc, ArrayList<TableData> _data) {
        super(_currentPoint, _p, _testPoints, _someFunc, _data);
    }

    @Override
    public void Run() {
        super.Run();
    }
}
